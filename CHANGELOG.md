# سجل التغييرات

جميع التغييرات المهمة في هذه الإضافة تُوثَّق هنا.

## [1.0.1] — 2026-08-20

إصدار إصلاحات. الأعراض الظاهرة كانت: ملفات Markdown تُفتح فارغة، والنقر على
روابط ملفات `.md` لا يفعل شيئاً، ورسالة `command 'rtlMd.makeDefault' not found`.
وقد نتجت عن أربع علل متراكبة، كل واحدة تحجب التي بعدها.

### أُصلح

- **الحزمة كانت تُبنى بلا أي مكتبة**: كان سكربت التغليف يستخدم
  `vsce package --no-dependencies`، فلا يُشحن `node_modules` إطلاقاً. النتيجة
  انهيار `Cannot find module 'markdown-it'` عند التحميل، وفشل التنشيط كاملاً —
  ولهذا لم تكن أي أوامر تُسجَّل. ملف `.vscodeignore` كان صحيحاً أصلاً وتستثني
  حزم التطوير وحدها؛ الراية كانت تُلغي عمله.
- **الإضافة لا تستيقظ عند الفتح الآلي**: أُضيف حدث التنشيط
  `onCustomEditor:rtlMd.reader`. كان التنشيط معتمداً على `onLanguage:markdown`
  وحده، وهو يأتي متأخراً عندما يفتح برنامج آخر ملف `.md` والمحرر المخصص هو
  الافتراضي، فيبقى التبويب فارغاً.
- **روابط الملفات ذات المسافات لا تُفتح**: كان `href` يُمرَّر إلى `Uri.joinPath`
  وهو ما يزال مُرمَّزاً، فيشير `%20` إلى مسار غير موجود. أُضيفت الدالة المشتركة
  `resolveLinkUri` التي تفكّ الترميز وتتعامل مع المسارات المطلقة وصيغة `file:`
  وأحرف أقراص Windows والـ fragments. ويتحقق الآن من وجود الملف قبل فتحه، لأن
  `vscode.open` يفشل بصمت على ملف مفقود فيبدو النقر كأنه لم يحدث.
- **الملفات خارج مجلد العمل تُفتح نصياً**: رُفعت أولوية المحرر المخصص من
  `option` إلى `default`، فلم يكن الربط يُطبَّق بشكل موثوق على ملف خارج مجلد
  العمل (مثل ملفات الخطط في `~/.claude/plans`).
- **`npm run package` كان يفشل**: سكربت prepublish يمرر مسار `tsc` غير مُقتبَس
  مع `shell: true`، فيقطعه cmd.exe عند أول مسافة في مسار المشروع.

### تغيّر

- المحرر المخصص صار افتراضياً لملفات Markdown (`priority: "default"`). للرجوع
  إلى المحرر النصي: زر «✎ تحرير» في شريط الأدوات، أو الأمر
  `RTL Markdown: إلغاء جعلها الافتراضية`.
- توحّد منطق فتح الروابط بين المحرر المخصص ولوحة المعاينة في دالة واحدة، بعد أن
  كانت نسختين متباعدتين تختلفان في السلوك.

### معروف

- عندما تفتح إضافة أخرى ملف `.md` عبر `showTextDocument` صراحةً (كما تفعل إضافة
  Claude Code مع روابط ملفات الخطط)، يُفتح الملف في المحرر النصي. الطلب الصريح
  يتجاوز `editorAssociations` و `priority`، ولا تستطيع أي إضافة منعه. الحل:
  <kbd>Ctrl</kbd>+<kbd>K</kbd> <kbd>V</kbd> لفتح المعاينة RTL بجانبه، أو
  `Reopen Editor With…` من قائمة التبويب.
- حجم الحزمة كبير (نحو 15 MB) لأن المكتبات تُشحن كما هي دون تحزيم.

## [1.0.0] — 2026-08-15

أول إصدار.

### أُضيف

- **عرض Markdown بالاتجاه من اليمين إلى اليسار** مع دعم كامل لخصائص Markdown.
- **الجداول**: المحاذاة الصريحة `|:---:|`، الدمج rowspan/colspan، الجداول
  متعددة الأسطر، والجداول بلا رؤوس.
- **معالجة الاتجاه على مستوى الرموز**: كل خلية جدول وفقرة وعنوان تحصل على
  `dir="auto"`، فيبقى النص اللاتيني داخل مستند عربي باتجاهه الصحيح.
- **كتل الكود والمعادلات تبقى LTR** عبر `unicode-bidi: isolate`.
- **محرر مخصص** (`rtlMd.reader`) يمكن جعله الافتراضي لملفات `.md` عبر الأمر
  `RTL Markdown: جعلها المعاينة الافتراضية لملفات MD` أو <kbd>Ctrl</kbd>+<kbd>Alt</kbd>+<kbd>R</kbd>.
- **تطبيق RTL على المعاينة المدمجة** في VS Code تلقائياً، عبر
  `markdownItPlugins` و `previewStyles`.
- مخططات **Mermaid** ومعادلات **KaTeX** (مضمّنة محلياً، تعمل دون إنترنت).
- الحواشي السفلية، قوائم المهام، قوائم التعريف، `~~حذف~~`، `==تمييز==`، `<ins>`.
- مزامنة التمرير بين المحرر والمعاينة في الاتجاهين.
- فهرس محتويات جانبي، وشريط أدوات (تحرير، تبديل الاتجاه، طباعة، تكبير/تصغير).
- تصدير إلى HTML مستقل، وطباعة/حفظ PDF.
- ١٥ إعداداً للتحكم في الخط والحجم والعرض والاتجاه وتفعيل الميزات.
- كشف تلقائي للاتجاه (`rtlMd.direction: "auto"`) يتجاهل كتل الكود عند التحليل.
